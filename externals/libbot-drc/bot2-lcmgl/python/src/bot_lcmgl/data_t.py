"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class data_t(object):
    __slots__ = ["name", "scene", "sequence", "datalen", "data"]

    def __init__(self):
        self.name = ""
        self.scene = 0
        self.sequence = 0
        self.datalen = 0
        self.data = ""

    def encode(self):
        buf = BytesIO()
        buf.write(data_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __name_encoded = self.name.encode('utf-8')
        buf.write(struct.pack('>I', len(__name_encoded)+1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">iii", self.scene, self.sequence, self.datalen))
        buf.write(bytearray(self.data[:self.datalen]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != data_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return data_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = data_t()
        __name_len = struct.unpack('>I', buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode('utf-8', 'replace')
        self.scene, self.sequence, self.datalen = struct.unpack(">iii", buf.read(12))
        self.data = buf.read(self.datalen)
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if data_t in parents: return 0
        tmphash = (0xe4f854412b5c6ecd) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if data_t._packed_fingerprint is None:
            data_t._packed_fingerprint = struct.pack(">Q", data_t._get_hash_recursive([]))
        return data_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

